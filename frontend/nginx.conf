server {
    listen 4173;

    # Serve the Svelte SPA
    root /usr/share/nginx/html;
    index index.html;

    # All non-asset routes fall back to index.html (client-side routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
